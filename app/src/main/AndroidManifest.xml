<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bfit22.fernando.com.bfit">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".PrincipalActivity" />
        <activity android:name=".ImcActivity" />
        <activity android:name=".SobrePesoActivity" />
        <activity android:name=".ObesidadActivity" />
        <activity android:name=".NormalActivity" />
        <activity android:name=".BajoPesoActivity" />
        <activity android:name=".RutinaAleatoria" />
        <activity android:name=".MainActivityPodometro" />
        <activity android:name=".MePicaUnHuevo"></activity>
    </application>

</manifest>